# Make package.json's `engines` a refusal to install rather than a comment. Without this, a
# contributor on Node 20 or 23 installs cleanly and then hits an opaque vitest failure with
# nothing pointing at the cause. It also covers CI, which deliberately runs no setup-node --
# see the note in release.yml -- and so tests against whatever Node the runner image ships.
engine-strict=true
