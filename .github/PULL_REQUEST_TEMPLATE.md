<!--
Thanks for this. CONTRIBUTING.md has the details; this is the short version.

Delete any section that doesn't apply — an empty heading is worse than no heading.
-->

## What this changes

<!-- From the user's side, not the code's. What can someone do now that they couldn't before,
     or what stops going wrong? -->

## Why

<!-- The reasoning that isn't obvious from the diff. If it fixes an issue, "Fixes #123". -->

## How it was checked

<!-- Not "CI is green" — CI runs `npm run build`, both test suites and clippy, and the PR page
     already says whether those passed. What did YOU do that CI can't?

     A UI change in particular is not verified by a type-check. Say you ran the app and what
     you clicked. If you couldn't run it (macOS or Linux — the Rust half won't build), say that
     instead; it's useful to know. -->

- [ ] Added or updated a test for the behaviour change, or it genuinely doesn't have one (say why)
- [ ] One concern per commit, subjects in the imperative
